const uploadlogo = (req, res) => {
  const logoHandler = req.body.logoHandler;
};

const editlogo = (req, res) => {
  const logoHandler = req.body.logoHandler;
};

module.exports = { uploadlogo, editlogo };
